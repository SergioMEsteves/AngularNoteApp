import { PlaceholderDirective } from './placeholder.directive';

describe('PlaceholderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
